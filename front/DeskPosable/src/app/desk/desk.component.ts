import { Component } from '@angular/core';

@Component({
    selector: 'desk'
})

export class DeskComponent {
    id :            number;
    x :             number;
    y :             number;
    orientation :   number;
    mapId :         number;
    personId :      number;
}